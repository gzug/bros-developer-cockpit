# Project Lead Prompt Versions

Rule: every future adjustment of the PL prompt must create one new version file and one changelog row. Use at most one prompt version per session.

| Version | Date       | What changed                                                                                                                                                                             | Why                                                                                                                                                                        | Expected effect                                                                                                                    |
| ------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| v1      | 2026-07-16 | Snapshotted the current `docs/HANDOFF.md` project lead prompt into `docs/prompts/pl-prompt-v1.md`.                                                                                       | Resume prompt versioning on top of the BDC eval-framework concept.                                                                                                         | Establish a stable baseline for later prompt-effect comparisons.                                                                   |
| v2      | 2026-07-16 | Superseded v1 with a pointer to the canonical current session-start prompt (01-One-L1fe mission board) + a Codex-fallback delta; banner-marked v1 stale.                                 | v1 had gone false after the re-orientation: it claimed the app is live and described retired lanes (batch gate, bdc-hold, reviewer). A single current source avoids drift. | Future PLs read the true current state (NOT READY, board frozen, APK-first), not a stale "live" prompt.                            |
| v3      | 2026-07-18 | Superseded v2's dead `01-One-L1fe/docs/planning/next-session-mission-board.md` pointer with the live canon: `session/START.md`, `AGENTS.md`, `CHECKPOINT.md`, and `git log origin/main`. | The mission-board document no longer exists, so v2 sends PLs to a dead source. The prompt must point to living truth files without copying One L1fe content into BDC.      | Future PLs start from current One L1fe truth and verify the latest main history instead of relying on a stale planning-board path. |
