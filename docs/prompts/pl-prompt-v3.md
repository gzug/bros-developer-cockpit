# Project lead prompt - v3 (supersedes v2)

`pl-prompt-v1.md` and `pl-prompt-v2.md` are **STALE** and must not be used.

v2 pointed to `01-One-L1fe/docs/planning/next-session-mission-board.md`. That file no longer exists, so the current PL start source is the living canon inside the `01-One-L1fe` repo.

## Canon to read first

In `01-One-L1fe`, read these sources in this order before making a plan or assigning work:

1. `session/START.md`
2. `AGENTS.md`
3. `CHECKPOINT.md`
4. `git log origin/main`

Use those files and the current Git history as the source of truth. Do not assume an older mission board, handoff, or prompt copy is current if it conflicts with them.

## Codex-fallback delta

If Codex, rather than Claude, steps in as PL: it has no `/grill` skill and no in-chat question widget. Ask owner decisions as plain-text options with a recommended default. Everything else follows the live canon above.
